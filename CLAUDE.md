# intake-suite

## Workflow

After pushing changes to a feature branch, always:
1. Create a pull request if one doesn't already exist (ready for review, not draft).
2. Merge the PR to main immediately after creating it.
